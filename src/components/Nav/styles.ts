import styled from 'styled-components';

export const Title = styled.span`
    font-size: 1.3rem;
    cursor: pointer;
`;

export const UnderLine = styled.li`
    &:hover {
        border-bottom: 2px solid var(--light-blue);
    }
`;
